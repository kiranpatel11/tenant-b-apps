apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  labels:
    app: {{ .Values.appName }}
    app.kubernetes.io/managed-by: tenant-b-apps
    platform.ocp-gitops.io/tenant: tenant-b
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
    - name: http
      port: 8080
      targetPort: http
